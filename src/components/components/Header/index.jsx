import styled from "styled-components";

const HeaderWrapper = () => {
  return (
    <Header>
      <p>Header</p>
    </Header>
  );
};

const Header = styled.header`
  height: 54px;
  background: #210037;
`;

export default HeaderWrapper;
